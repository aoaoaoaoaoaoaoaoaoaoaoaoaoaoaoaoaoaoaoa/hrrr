[package]
name = "hrrr-acceptance"
version = "0.1.0"
description = "Hermetic native acceptance stories for HRRR"
edition = "2024"
license = "MIT"
repository = "https://github.com/aoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoa/hrrr"
rust-version = "1.96"
publish = false

[dependencies]
egui-tester = "0.7.0"
fast-mvt = { version = "0.6.1", default-features = false, features = ["writer"] }
geo-types = "0.7.19"
hrrr-contract = { path = "../hrrr-contract", version = "0.5.0" }
pmtiles = { version = "0.23.1", default-features = false, features = ["write"] }
serde = { version = "1.0.228", features = ["derive"] }
toml = "1.1.2"

[lints]
workspace = true
