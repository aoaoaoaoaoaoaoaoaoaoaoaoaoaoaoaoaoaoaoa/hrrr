name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

jobs:
  foundry:
    name: Foundry
    permissions:
      contents: read
    uses: aoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoa/eternalist-foundry/.github/workflows/pipeline.yml@9e9f8a5c0d52c2a99f1f3baefdfac921a3eb457e

  registry:
    name: Registry
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    needs: foundry
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Require Cargo publication
        env:
          VERSION: ${{ github.ref_name }}
        run: cargo info --registry crates-io "hrrr@${VERSION#v}" >/dev/null

  release:
    name: Release
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    needs: [foundry, registry]
    permissions:
      contents: write
    uses: aoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoaoa/eternalist-foundry/.github/workflows/publish.yml@9e9f8a5c0d52c2a99f1f3baefdfac921a3eb457e
